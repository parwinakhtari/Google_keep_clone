import React from "react";
import LockIcon from "@material-ui/icons/Lock";

function Header() {
  return (
    <header>
      <h1>
        <LockIcon />
        Google Keep Clone
      </h1>
    </header>
  );
}

export default Header;
